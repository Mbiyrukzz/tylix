// No runtime values — this module exists purely so its .d.ts sibling
// can export shared query-shape types (WhereClause, QueryDescriptor)
// referenced by QueryBuilder and DatabaseAdapter.
export {}
