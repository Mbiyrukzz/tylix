
capability Mail

state
  unread: 0

action
  async load() {
    const result = await useApi("/api/_tylix/mail-logs")
    if result.ok {
      this.unread = result.data.data.length
    }
  }

init
  async {
    await this.load()
  }