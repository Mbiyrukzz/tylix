// Compiler has no project-specific scaffold-time settings yet — the
// `compiler: {}` block in tylix.config.js.template is a placeholder
// for future options (e.g. JSX pragma, target). Nothing to write today.
export async function writeCompilerConfig(config) {
  return
}
