// ORM config is driven entirely by `database.driver` in tylix.config.js,
// which writeDatabaseConfig.js already writes. The `orm: {}` block is a
// placeholder for future per-driver options. Nothing to write today.
export async function writeOrmConfig(config) {
  return
}
